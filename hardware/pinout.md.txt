# Wiring & Pinout

## ESP32 Flight Controller
| Pin | Function |
|-----|----------|
| GPIO32 | Motor 1 (BR, CW) — DShot300 |
| GPIO33 | Motor 2 (TR, CCW) — DShot300 |
| GPIO25 | Motor 3 (BL, CCW) — DShot300 |
| GPIO4  | Motor 4 (TL, CW) — DShot300 |
| GPIO21 | I2C SDA → MPU6050 |
| GPIO22 | I2C SCL → MPU6050 (400kHz) |
| GPIO27 | UART2 RX ← Pi flow link (115200) |

## Transmitter ESP32
| Pin | Function |
|-----|----------|
| GPIO33 | Throttle (inverted) |
| GPIO32 | Yaw |
| GPIO35 | Pitch (inverted) |
| GPIO34 | Roll |
| GPIO16 | Arm switch (INPUT_PULLUP, LOW = armed) |

Stick centers: THR 2128 · YAW 1909 · PITCH 2173 · ROLL 1968

## Raspberry Pi 3B+
| Pin | Function |
|-----|----------|
| Pin 8 (GPIO14/TXD) | → ESP32 GPIO16 (flow packets) |
| Pin 6 (GND) | common ground |
| 5V via GPIO header | ← buck converter (bypasses input fuse) |
| CSI | OV5647 camera, M12 lens, focused @ ~1.5m |

## Radio
- Drone ↔ TX: ESP-NOW. FC MAC: B4:BF:E9:60:89:D0
- Control 50Hz up · telemetry 10Hz down
- Will add antenna

## Flow packet (Pi → FC)
115200 baud, 30Hz, one-directional (Pi transmits, FC listens)

| Byte | Field | Notes |
|------|-------|-------|
| 0-1  | sync  | 0xAA 0x55 |
| 2-3  | velX  | int16, little-endian, pixels/frame × 100 |
| 4-5  | velY  | int16, little-endian, pixels/frame × 100 |
| 6    | quality | uint8, tracked feature count |
| 7    | flags | bit0 = valid (quality >= MIN_FEATURES) |
| 8    | seq   | rolling counter |
| 9    | cksum | XOR of bytes 2-8 |

**Total: 10 bytes.** FC treats data as stale after 200ms without a valid packet
(`flowFresh` gate) and falls back to unassisted flight.

⚠ Originally mis-specced as 12 bytes — the FC parser waited for a frame length
that never arrived, so every packet was silently dropped. Cost several days.
